// Tauri serves a static single-page app: no server-side rendering, and we
// prerender the shell so the bundle works when loaded from the local filesystem.
export const prerender = true;
export const ssr = false;
